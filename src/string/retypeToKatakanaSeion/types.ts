export type RetypeToKatakanaSeionOptions = {
  /**
   * 濁音は変換しない
   * @default false
   */
  disableDakuon?: boolean;

  /**
   * 促音は変換しない
   * @default false
   */
  disableSokuon?: boolean;

  /**
   * 拗音は変換しない
   * @default false
   */
  disableYouon?: boolean;
};
